// to-do: implementar teste
// const test  = require("node:test")
// const assert = require("node:assert")

// const { split } = require("./extractMessage")

// test('split do texto pelo delimitador', () => {
//   const mailText = "minha mensagem\n___\nresto do email";
//   const delimiter = "___";
//   const result = split(mailText, delimiter);
//   assert.strictEqual(result, mailText);
// });

// test('returna o texto completo', () => {
//   const mailText = "minha mensagem sem delimitador";
//   const delimiter = "___";
//   const result = split(mailText, delimiter);
//   assert.strictEqual(result, mailText);
// });